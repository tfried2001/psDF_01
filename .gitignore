# git ls-files --others --exclude-from=.git/info/exclude
# Lines that start with '#' are comments.
# For a project mostly in C, the following would be a good set of
# exclude patterns (uncomment them if you want to use them):
# *.[oa]
# *~

# Grabbed from github.com/github/gitignore

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# PyInstaller generated files (unique every time project is installed)
*.manifest
*.spec

# venv Virtual Environments (clashes happen syncing between win/linux environments)
psDF/
# For venv compatibility, listing packages here:
# python -m venv psDF
# python -m pip install django
# python -m pip install django-crispy-forms
# python -m pip install pylint

# Local History VSCode Extension
.history/


# Useful Powershell line for removing cached files that hadn't yet been added to ignore
# git ls-files -c '*/__pycache__/*' | %{git rm --cached $_}
